/**
 * Convex Authentication Configuration
 *
 * For now, we're using custom JWT auth via AuthManager on the FiveM side.
 * This file can be extended later if you want to use Convex's built-in auth.
 */

export default {
    providers: [],
};

/**
 * To add Convex auth later, see:
 * https://docs.convex.dev/auth/config
 */
